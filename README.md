# arch-be — Spine Banking Edition (дистрибутив)

Доменный агент-харнесс **solution-архитектора банка**: один статический
бинарь для Linux x86-64 (зависимостей нет). Внутри — дисциплина
архитектурных решений: ADR, architecture-spine инварианты, рубрики
качества с LLM-судьёй, фитнес-функции, handoff-пакеты кодовым агентам,
база знаний, библиотека скиллов, фоновые суб-агенты.

Работает с **китайскими LLM** (DeepSeek / Z.AI GLM / Kimi — прямой доступ
из РФ, без VPN) и с **локальными моделями** в закрытом контуре
(llama.cpp / vLLM / Ollama / LM Studio / корпоративный шлюз, в т.ч. веса
с HuggingFace).

## Платформы

- **Linux x86-64** — нативно, основная платформа.
- **Windows** — только через **WSL2** (Ubuntu): внутри WSL вся инструкция
  выполняется дословно.
- **macOS** — нативной сборки нет: харнесс ставится на Linux-машину или в
  Linux-VM (UTM/Parallels, на Apple Silicon — с Rosetta). Сервер локальной
  модели при этом может жить на самом Маке (Ollama, llama.cpp, LM Studio —
  ускорение Metal из коробки).

## Быстрый старт (5 минут)

Скачайте из [последнего релиза](../../releases/latest) три файла:
`arch-be-linux-x86_64` (бинарь), `deploy-kit-v0.1.5.tar.gz`
(инструкции + стартовый конфиг) и `archify-vendored-2.17.0-dev.1.tar.gz`
(движок диаграмм Archify, закреплённая версия). Затем:

```bash
sha256sum -c SHA256SUMS.txt                 # сверка целостности — все строки «OK»
cp arch-be-linux-x86_64 ~/.local/bin/arch-be && chmod +x ~/.local/bin/arch-be
arch-be init                                # конфиг + ассеты в ~/.arch-harness
export DEEPSEEK_API_KEY="sk-..."            # ваш ЛИЧНЫЙ ключ: platform.deepseek.com
arch-be run -q "Привет! Кто ты?"            # проверка; дальше: arch-be (интерактивный TUI)
```

**Диаграммы Archify** (понадобится Node.js >= 18): инструменты зашиты в
бинарь, движок ставится из вендоренного tarball'а релиза — с сетевого
`npx` не ставьте, там непроверенный latest:

```bash
tar xzf archify-vendored-2.17.0-dev.1.tar.gz -C ~/.arch-harness/
# в ~/.config/arch-harness/config.toml → [archify]:
# cli_path = "/home/<user>/.arch-harness/archify/bin/archify.mjs"  (абсолютный путь, ~ не работает)
arch-be doctor                              # ✓ archify node ... + CLI ... (версия — проверенная)
```

**Бесплатный старт без банковской карты:** регистрация на
[z.ai](https://z.ai) по email (карта не нужна, пока пользуетесь
бесплатными flash-моделями) → `export ZHIPU_API_KEY` → одна строка
`default_model = "glm-flash"` в `~/.config/arch-harness/config.toml`.
Сценарий проверен живьём 2026-09-08.

## Документы комплекта

| Файл | Что внутри |
|---|---|
| [КОЛЛЕГАМ.md](КОЛЛЕГАМ.md) | Титульная записка: старт за 5 минут, правила гигиены ключей |
| [deploy-kit/ИНСТРУКЦИЯ_ЧЕЛОВЕК.md](deploy-kit/ИНСТРУКЦИЯ_ЧЕЛОВЕК.md) | Полная пошаговая инструкция: установка, ключи, все модели, локальные LLM на разных ОС, контекстные папки, таблица граблей |
| [deploy-kit/ИНСТРУКЦИЯ_АГЕНТ.md](deploy-kit/ИНСТРУКЦИЯ_АГЕНТ.md) | Контракт для ИИ-агента, который разворачивает харнесс за вас |
| [deploy-kit/config.starter.toml](deploy-kit/config.starter.toml) | Готовый минимальный конфиг: DeepSeek + GLM + Kimi + локальная модель |
| [deploy-kit/ПРОВЕРКА.md](deploy-kit/ПРОВЕРКА.md) | Протокол живого тестирования (6 волн) |
| [vendor/archify/](vendor/archify/) | Вендоренный движок диаграмм Archify 2.17.0-dev.1 (источник tarball'а релиза; MIT, © tt-a1i) |
| [SHA256SUMS.txt](SHA256SUMS.txt) | Контрольные суммы ассетов релиза |

Если настраивать будет ваш ИИ-агент — передайте ему каталог deploy-kit
и скажите: «Действуй по `ИНСТРУКЦИЯ_АГЕНТ.md`, шаг за шагом, ключи спроси
у меня».

## Что уже зашито в бинарь

Ничего докачивать не нужно — `arch-be init` раскладывает встроенное:

- **7 плагинов / 52 скилла** (arch-core, arch-office, patterns-integration,
  patterns-resilience, aws-agentic-ai, aws-builders-library, spine-be-docs);
- **9 промптов**, **6 рубрик качества**, **6 бенчмарков** (+ golden);
- дефолтные секции моделей: deepseek, deepseek-pro, glm, glm-4.7, glm-air,
  glm-flash, glm-5.3-flash, kimi, gigachat* — работают сразу после
  установки ключа в окружение;
- 3 MCP-сервера, примеры cron и fitness-правил (CONSTRAINTS);
- инструменты диаграмм `archify_show/validate/deliver/compare` — но
  движок Archify (Node.js CLI) в бинарь НЕ зашит: он идёт отдельным
  вендоренным tarball'ом релиза с закреплённой версией
  (`archify-vendored-2.17.0-dev.1.tar.gz`, проверенная с харнессом по
  контракту `schemaVersion: 1`). Установка — 2 команды из быстрого старта.

Своё наращивается без пересборки: база знаний (`[knowledge].dirs` +
`arch-be kb`), свои плагины (`[plugins].dirs`), глобальная память
(`~/.arch-harness/MEMORY.md`).

## Проверено живьём

Инструкции и дистрибутив прошли **6 волн тестирования** (протокол —
[ПРОВЕРКА.md](deploy-kit/ПРОВЕРКА.md)), последняя — 2026-09-08:

- установка с нуля в изолированных окружениях, sha256-сверка релиза;
- китайские API: 5 моделей (включая прямой доступ из РФ без прокси) и
  сценарий «только бесплатный ключ GLM» end-to-end;
- локальные LLM: llama.cpp и Ollama на GPU (включая вызовы инструментов),
  vLLM (протокол tools), корпоративный шлюз в приватной сети;
- интерактивный TUI и строгий headless-контракт `run -q`;
- контекстные каналы: kb без переиндексации, свой плагин, MEMORY.md.

## Провенанс

Собрано из приватного репозитория `spine-private`, ветка `spine-be`
(та же версия публикуется там тем же тегом; бинарь идентичен побайтно).
Тесты исходника: 974 passed, 0 failed.
