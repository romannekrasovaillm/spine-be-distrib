# Протокол проверки инструкции (2026-09-07)

Инструкция прогнана «с нуля» в изолированном окружении: чистый HOME
(`/tmp/archbe-kit-test`), в него скопирован только бинарь `arch-be`
(сборка `target/release` на 2026-09-07, v0.1.0). Реальный конфиг и
реальные `~/.arch-harness` пользователя не затрагивались. Ключи брались
из окружения/файлов машины через shell-подстановку, значения нигде не
печатались.

## Развёртывание

| # | Шаг | Команда | Результат |
|---|---|---|---|
| 1 | Копия бинаря | `cp arch-be <sandbox>/` | OK |
| 2 | Инициализация | `arch-be init` | OK: созданы `~/.config/arch-harness/config.toml`, `~/.arch-harness/{assets,plugins,mcp.json,cron.toml,CONSTRAINTS.example.yaml}` |
| 3 | Диагностика | `arch-be doctor` | OK: «Итог: 0 проблем, 3 предупреждения из 11 проверок» (ключи неиспользуемых моделей, placeholder-каталоги knowledge, archify — допустимые) |
| 4 | Плагины из бинаря | `arch-be doctor` | OK: «7 плагинов, 52 скиллов» развернуты `init` |

## Китайские LLM (живые вызовы, headless-контракт `run -q`)

| Модель | Endpoint | Команда | Ответ | Exit |
|---|---|---|---|---|
| deepseek (v4-flash) | api.deepseek.com (напрямую из РФ) | `arch-be run -q "…столица Франции?"` | «Париж» | 0 |
| glm (glm-5.2) | api.z.ai (с env-прокси) | `arch-be run -q --model glm "…столица Японии?"` | «Токио.» | 0 |
| glm (glm-5.2) | api.z.ai (прокси сняты, прямое соединение) | `env -u http_proxy … arch-be run -q --model glm "…2+2=?"` | «4» | 0 |
| glm-flash (glm-4.7-flash, бесплатный тариф) | api.z.ai | `arch-be run -q --model glm-flash "…столица Германии?"` | «Берлин» | 0 |
| kimi (k3) | api.kimi.com/coding/v1 | `arch-be run -q --model kimi "…столица Китая?"` | «Пекин» | 0 |

## Локальная / self-hosted модель

| Сценарий | Результат |
|---|---|
| Ollama `http://127.0.0.1:11434/v1`, модель без поддержки tools | Ожидаемый отказ: `HTTP 400 … does not support tools`, exit 1 — задокументировано как требование «модель с tool calls» |
| Корпоративный шлюз в приватной сети (keyless), `qwen3.8-27b@2026-08-18@llama.cpp` | OK: «Рим», exit 0. Паттерн `api_key_env` = любая установленная переменная подтверждён |

## Контекстные папки

| Проверка | Результат |
|---|---|
| `[knowledge].dirs` → свой каталог с 2 md | `arch-be kb "saga компенсация транзакции"` → хит `saga.md:3 (score 5.6)` с цитатой |
| Нюанс init | `arch-be init` пишет в конфиг placeholder-каталоги knowledge (развёрнутые абсолютные пути); doctor честно показывает «0 из 3 каталогов доступны» — задокументировано в инструкции |
| `config.starter.toml` из комплекта | Загружается: `arch-be --config config.starter.toml models` — 5 моделей; `doctor` — «0 проблем» |

## Не проверялось

- GigaChat (нет ключа на тестовом стенде; не китайский провайдер — вне задания).
- glm-5.3-flash (секция есть в дефолтах бинаря; в starter-конфиг не включена).
- TUI (интерактив); проверялся строгий headless-контракт `run -q`.

## Аудит дружелюбности, вторая волна (2026-09-07)

Проверены сценарии трения нового пользователя; по итогам инструкции
уточнены (имя файла релиза, exec-бит, PATH, default_model):

| Сценарий | Результат |
|---|---|
| Запуск вообще без `arch-be init` (только встроенные дефолты) | LLM отвечает («Мадрид», exit 0), но doctor: «0 плагинов, 0 скиллов», 1 проблема — `init` обязателен, в инструкции это теперь сказано явно |
| Файл из релиза GitHub называется `arch-be-linux-x86_64`, без exec-бита | Инструкции давали `cp arch-be …` — исправлено: оба пути получения (релиз/клон), `sha256sum`-сверка, `chmod +x`, замечание про PATH |
| У пользователя только ключ GLM/Kimi (нет DeepSeek) | Первый прогон упирался бы в дефолт `deepseek` — добавлен блок про `default_model`. Полный ре-проход в чистом HOME без DEEPSEEK_API_KEY: sha256 → install → init → `default_model = "glm"` → doctor «0 проблем» → `arch-be run -q` → «Осло», exit 0 |
| Релиз не самодостаточен (доки только в приватном репо) | К релизу приложен `deploy-kit-v0.1.0.tar.gz` (инструкции + starter-конфиг, без бинаря) |

## Третья волна: локальная модель с HuggingFace end-to-end (2026-09-07)

Полная цепочка «веса с HF → свой сервер → arch-be» в чистом HOME:

| Шаг | Результат |
|---|---|
| Доступ к HF: напрямую из РФ | Сбой соединения (DPI по SNI); ранее в тот же день флапало — в инструкции описаны зеркало hf-mirror.com, HTTPS_PROXY, ModelScope, ollama pull |
| `huggingface-cli download` | Первая попытка упала: «Unknown scheme for proxy URL socks://…» (hf-клиент не понимает socks из ALL_PROXY) — грабля задокументирована; с http-прокси через VPN-egress — скачан `Qwen/Qwen2.5-0.5B-Instruct-GGUF` (q4_k_m, ~400 МБ за ~2,5 мин) |
| `llama-server -m … -c 8192 --jinja` | arch-be упал: `HTTP 400 exceed_context_size_error` — промпт харнесса с инструментами 13 913 токенов > 8192. Вывод в инструкции: контекст сервера — от 32K |
| `llama-server -m … -c 32768 --jinja` + `[models.hf-local]` | OK: «Рим», exit 0 |
| Tool calls на уровне протокола (curl с `tools`) | OK: `finish_reason=tool_calls`, корректный JSON аргументов |
| Агентная задача на 0.5B («прочитай файл инструментом») | Модель НЕ вызвала инструмент, ответила невпопад — ограничение размера модели, не цепочки; в инструкции рекомендация «от ~7B» |

Вывод: цепочка HF → llama.cpp → arch-be работает; мелкие модели годятся
для проверки связки, для агентной работы — от ~7B (Qwen3+).

## Четвёртая волна: тотальный аудит обеих инструкций (2026-09-07)

Полная сверка каждой команды и каждого факта + буквальный повторный
проход обеих инструкций в двух чистых HOME. Найдено и исправлено:

| # | Находка | Критичность | Исправление |
|---|---|---|---|
| 1 | Путь Б (git clone): ветка по умолчанию репо — `main` (другая продуктовая линия), deploy-kit живёт в `spine-be` → `cd spine-private/deploy-kit` падал бы | КРИТИЧНО | `git clone -b spine-be`; проверено реальным клоном |
| 2 | Путь Б обещал «здесь бинарь arch-be», но бинарь в git не хранится (gitignore, релиз-артефакт) → в клоне бинаря нет | ВЫСОКАЯ | Явно написано: бинарь — из релиза, в клоне только доки/конфиг |
| 3 | Ссылки на «релиз v0.1.0» и `deploy-kit-v0.1.0.tar.gz` устарели | СРЕДНЯЯ | «последний релиз», `deploy-kit-v0.1.2.tar.gz` (далее версионно) |
| 4 | Прямой доступ из РФ к DeepSeek и Kimi ранее был проверен только через прокси; днём прямой egress флапал | СРЕДНЯЯ | Перепроверено с ПОЛНОСТЬЮ снятыми прокси: DeepSeek «6», Kimi «10», exit 0; в обеих инструкциях добавлен запасной вариант — поле `proxy` в секции модели |
| 5 | `ollama serve` после `ollama create` упал бы с "address in use" при уже запущенном демоне | НИЗКАЯ | Пометка: повторный serve не нужен, пропустить |

Сверено с кодом/реальностью без изменений (всё сошлось): дефолтные
модели и лимиты из `src/config.rs` (deepseek v4-flash/v4-pro 1M, kimi k3
1M + api_key_file, glm-5.2 1M, glm-4.7 204800, glm-air 131072,
glm-flash 204800, glm-5.3-flash 1310720 + reasoning_effort=low);
субкоманды `plugins list`/`skills list`/`memory` и слэши `/model`,
`/think`, `/memory`; состав ассетов (9 промптов, 6 рубрик, 6 бенчмарков,
7 плагинов/52 скилла); эталонные строки doctor.

Буквальный проход инструкции ЧЕЛОВЕКА (чистый HOME, путь А из тарбола):
tar → sha256 (совпал с релизным b5eeee56…) → install → `arch-be -V` →
init → doctor «Итог: 0 проблем» → `run -q` «Париж» exit 0 →
`plugins list` (7) → `skills list` — всё OK.

Буквальный проход инструкции АГЕНТА (второй чистый HOME, шаги 1–7
дословно): install → init → проверка ключей → doctor «0 проблем» →
приёмка трёх моделей ДОСЛОВНЫМИ командами из инструкции (deepseek
«Париж», glm «Токио», kimi «Пекин»; exit 0) → kb-хит по своей папке →
`config.starter.toml` загружается (5 моделей) — всё OK.

## Пятая волна: произвольное железо коллеги + контекст (2026-09-08)

Перепроверка сценария «коллега разворачивает с нуля на любом железе» +
приёмка китайских API и контекстных каналов в свежем чистом HOME
(`/tmp/archbe-recheck-0908`, бинарь из релиза v0.1.3, sha256 сверен с
SHA256SUMS.txt).

Китайские API (дословные команды инструкции, все exit 0): deepseek
«Париж», glm «Токио», kimi «Пекин», glm-flash «Берлин», glm-5.3-flash
«Рим» (впервые). Прямой доступ из РФ с ПОЛНОСТЬЮ снятыми прокси
подтверждён повторно: deepseek «Мадрид», glm «Осло», kimi «Сеул».

Локальный инференс на локальном GPU (RTX 4080 SUPER, драйвер 560):

| Движок | Результат |
|---|---|
| llama.cpp CUDA (GGUF 4B, `-c 32768 --jinja`) | OK: arch-be «Рим» exit 0; tools на уровне протокола корректны (`finish_reason=tool_calls`, валидный JSON) |
| Ollama `qwen3:8b` (дословная команда `ollama pull`) | OK: arch-be «Рим» exit 0; агентный прогон — в логе сессии реальный вызов `read_file` и получение содержимого файла |
| Ollama, модель без tool-шаблона | Ожидаемый отказ воспроизведён: `HTTP 400 ... does not support tools`, exit 1 |
| vLLM 0.6.3 (Qwen2.5-0.5B-Instruct) | Сервер поднят; протокол OK (chat + tool_calls с валидными аргументами). Под полным промптом харнесса (~14K) 0.5B выдаёт мусор — согласуется с рекомендацией «агентная работа от ~7B» |

Найдено и исправлено в инструкциях:

| # | Находка | Критичность | Исправление |
|---|---|---|---|
| 1 | `huggingface-cli download` в huggingface_hub 1.x ОТКЛЮЧЁН («deprecated and no longer works», exit 0, скачивает ничего) — ломал шаг 2 «Модель с HF» у любого со свежим pip | КРИТИЧНО | В обеих инструкциях команда заменена на `hf download` (+пометка про старые версии <1.0); добавлена строка в «Типичные грабли» |
| 2 | Вариант B (vLLM) как был написан (`vllm serve <репо>`) → arch-be получает `HTTP 400 "auto" tool choice requires --enable-auto-tool-choice...` | ВЫСОКАЯ | Рабочая команда с `--enable-auto-tool-choice --tool-call-parser hermes --guided-decoding-backend lm-format-enforcer` + матрица версий (vllm 0.28 требует драйвер новее 560; под 560 — vllm==0.6.3 + transformers==4.45.2, т.к. >=4.46 падает с assert "factor" in rope_scaling; у outlines зависимость pyairports на PyPI — пустышка) |
| 3 | Сценарий Mac не описан вовсе: бинарь только linux-x86_64, а инференс на Маке рабочий (Ollama/brew llama.cpp/LM Studio, Metal); на macOS export'ы в `~/.zshrc` | ВЫСОКАЯ | Шаг 0 человека и шаг 1 агента: явно «харнесс — Linux, сервер модели — где угодно»; в инструкции человека добавлен подраздел «Если inference-сервер — на Маке» |
| 4 | Дубликат TOML-секции (вторая `[plugins]` в конце конфига) роняет харнесс с `duplicate key` | НИЗКАЯ | В обеих инструкциях: править существующие секции `[knowledge]`/`[plugins]`, не дописывать дубли; строка в «Типичные грабли» |
| 5 | Каналы скачивания весов: vpn-egress сегодня сталлит, hf-mirror напрямую из РФ недоступен; `registry.ollama.ai` напрямую — работает (5,2 ГБ за ~2 ч) | НИЗКАЯ | `ollama pull` помечен как самый беспроблемный канал из РФ |

Контекстные каналы (всё живьём в чистом HOME): база из коробки сверена
(7 плагинов/52 скилла, 9 промптов, 6 рубрик, 6 бенчмарков+golden, 3
MCP-сервера); наращивание — `arch-be kb` по новому файлу сразу после init
(без переиндексации), свой плагин `corebank` подхвачен (8 плагинов/53
скилла), `arch-be memory add` → модель воспроизвела кодовое слово из
MEMORY.md.

Не проверялось: реальный Мак (нет железа — Mac-раздел по документации
экосистемы); vLLM с моделью 7B+ (сеть: ~2 МБ/с, safetensors 8B не
выкачать за разумное время); Windows.

Послесловие к пятой волне: устранён некритичный рассинхрон версий —
бинарь печатал `arch-be 0.1.0` при релизе v0.1.3. Версия в Cargo.toml
поднята до 0.1.3, бинарь пересобран и перевыпущен в том же релизе
(sha256 в SHA256SUMS.txt обновлён), эталонные строки `arch-be -V` в
обеих инструкциях исправлены на 0.1.3. Приёмка нового бинаря в чистом
HOME: `-V` → 0.1.3, doctor «0 проблем», `run -q` → exit 0.
